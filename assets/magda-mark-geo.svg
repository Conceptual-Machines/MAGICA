<svg width="512" height="512" xmlns="http://www.w3.org/2000/svg">
  <g id="magda-mark">
    <!-- Central circle -->
    <circle cx="256" cy="256" r="36" fill="#808080"/>

    <!-- Four outer circles -->
    <!-- Top-left -->
    <circle cx="157" cy="157" r="24" fill="#808080"/>
    <!-- Top-right -->
    <circle cx="355" cy="157" r="24" fill="#808080"/>
    <!-- Bottom-left (wider, sloped down) -->
    <circle cx="100" cy="355" r="24" fill="#808080"/>
    <!-- Bottom-right (wider, sloped down) -->
    <circle cx="412" cy="355" r="24" fill="#808080"/>

    <!-- Four diagonal arms from center to outer circles -->
    <!-- Top-left arm: longer, but starting further from center to create gap -->
    <g transform="translate(195, 195) rotate(-135)">
      <rect x="-50" y="-10" width="100" height="20" rx="10" ry="10" fill="#808080"/>
    </g>

    <!-- Top-right arm: longer, but starting further from center to create gap -->
    <g transform="translate(317, 195) rotate(-45)">
      <rect x="-50" y="-10" width="100" height="20" rx="10" ry="10" fill="#808080"/>
    </g>

    <!-- Bottom-left arm: extended toward center (inner side) -->
    <g transform="translate(170, 320) rotate(147.5)">
      <rect x="-77.5" y="-10" width="150" height="20" rx="10" ry="10" fill="#808080"/>
    </g>

    <!-- Bottom-right arm: extended toward center (inner side) -->
    <g transform="translate(342, 320) rotate(32.5)">
      <rect x="-77.5" y="-10" width="150" height="20" rx="10" ry="10" fill="#808080"/>
    </g>

    <!-- Two side arms connecting top and bottom circles -->
    <!-- Left side arm: top-left (157,157) to bottom-left (100,355) -->
    <!-- Vector: (-57, 198), distance: 206.0, arm length: 158.0, angle: 106.1° -->
    <g transform="translate(128.5, 256) rotate(106.1)">
      <rect x="-79" y="-10" width="158" height="20" rx="10" ry="10" fill="#808080"/>
    </g>

    <!-- Right side arm: top-right (355,157) to bottom-right (412,355) -->
    <!-- Vector: (57, 198), distance: 206.0, arm length: 158.0, angle: 73.9° -->
    <g transform="translate(383.5, 256) rotate(73.9)">
      <rect x="-79" y="-10" width="158" height="20" rx="10" ry="10" fill="#808080"/>
    </g>
  </g>
</svg>
